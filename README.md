# Visitor Counter

A tiny Node.js app that:
- shows a webpage;
- counts visits;
- stores the count in Redis;
- runs with Docker Compose.

To learn:
- app countainers;
- Redis container;
- networking between containers;
- volumes;
- rebuilding images;
- environment variables.
